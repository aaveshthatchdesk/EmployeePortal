@page "/employeelist"
@using Application.Dtos
@using Application.Interfaces.IService
@using System.Security.Claims
@using Microsoft.AspNetCore.Components.Authorization

@inject AuthenticationStateProvider AuthStateProvider
@inject IEmployeeService _employeeService


<div class=container-fluid mt-3 >
    <div class="card shadow border-0 rounded-4 overflow-hidden">
        <div  class="card-body p-0">


            <div class="px-4 px-3 mt-3 d-flex justify-content-between  align-items-center ">
                <h3 class="fw-bold mb-2" style="color:#2c3e50;">Employees Management</h3>
                <div class="d-flex align-items-center  justify-content-between">
                <div class="position-relative mx-4">
                    <input class="form-control pe-5 shadow-sm"
                         placeholder="search tasks.."
                               @bind="searchTerm"
                               style=" background-color: #f1f3f4;
                   border-radius: 12px;
                   border: 1px solid #ddd;"
                               @oninput="@(async e => await SearchTasks(e))" />
                         
                    <span class="position-absolute top-50 end-0 translate-middle-y me-3 text-muted">
                        <i class="bi bi-search"></i>
                    </span>
                </div>
                 <button class="btn btn-add btn-sm px-3 shadow-sm d-flex align-items-center " style="border-radius: 8px; height:40px; background-color:#2c3e50; color:white;" >
                + Add Employee
            </button>
                </div>
            </div>

           
                <div class="container mt-4">
                    <div class="row g-3">
                        <div class="col-md-3">
                        <div class="card stat-card" style="background-color:#ecf0f1;border-radius:10px;">
                                <div class="card-body">

                         
                    <div class="d-flex justify-content-center align-items-center ">
                                    <i class="bi bi-list-task fs-2 text-primary"></i>

                        <h6 class="text-center ms-2">employess</h6>


                    </div>
                    <h3 class="fw-bold mt-2 text-center">Total Employees</h3>
                      </div>
                            </div>
                        </div>
                         <div class="col-md-3">
                        <div class="card stat-card" style="background-color:#ecf0f1;border-radius:10px;">
                                <div class="card-body">


                                    <div class="d-flex justify-content-center align-items-center">
                                    <i class="bi bi-play-circle fs-2 text-info"></i>
                                        <h6 class="text-center ms-2">ACTIVE </h6>


                                    </div>
                                    <h3 class="fw-bold mt-2 text-center">Active</h3>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-3">
                        <div class="card stat-card" style="background-color:#ecf0f1;border-radius:10px;">
                                <div class="card-body">


                                    <div class="d-flex justify-content-center align-items-center">
                                    <i class="bi bi-exclamation-triangle fs-2 text-danger"></i>
                                        <h6 class="text-center ms-2">On Leave</h6>


                                    </div>
                                    <h3 class="fw-bold mt-2 text-center">On Leave</h3>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-3">
                        <div class="card stat-card" style="background-color:#ecf0f1;border-radius:10px;">
                                <div class="card-body">


                                    <div class="d-flex justify-content-center align-items-center">
                                    <i class="bi bi-check-circle fs-2 text-success"></i>
                                        <h6 class="text-center ms-2">InActive</h6>


                                    </div>
                                    <h3 class="fw-bold mt-2 text-center">Inactive</h3>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                
            <div class="container mt-5">
                <div class="card shadow-sm border-0 px-4  py-3" style="background-color:#ecf0f1;border-radius:10px;">
                    
                        <h5 class="fw-bold mb-0">All Tasks</h5>

                        <table class="table mt-4 ">
                            <thead>
                                <tr>
                                <th style="background-color:#2c3e50;color:white">Sr.No</th>

                                <th style="background-color:#2c3e50;color:white">Employee</th>
                                <th style="background-color:#2c3e50;color:white">Department</th>
                                  
                                <th style="background-color:#2c3e50;color:white" >Position</th>
                                <th style="background-color:#2c3e50;color:white">Status</th>
                                <th style="background-color:#2c3e50;color:white">Joining Date</th>
                                <th style="background-color:#2c3e50;color:white">Actions</th>
                            </tr>
                            </thead>
                            <tbody>
                         
                         @*       @if (isTasksLoading)
                         {
                                <tr>
                                    <td colspan="5" class="text-center py-4">
                                        <div class="spinner-border text-primary" role="status"></div>
                                        <div class="mt-2 text-secondary">Loading members...</div>
                                    </td>
                                </tr>
          
                             } *@
                           
                           @* else if (pagedTasks == null || !pagedTasks.Any())
                                 {
                                <tr><td colspan="5" class="text-center text-muted py-3">No members found.</td></tr>
                                  }
                                  else *@
                                         @* { *@
                                     
                              
                                @foreach (var (employee ,index) in pagedEmployees.Select((t, i) => (t, (currentPage - 1) * pageSize + i + 1)))
                                {
                                    <tr class="task-row">
                                        
                                    <td class="align-middle text-center">@index</td>
                                    <td class="task-desk-cell">
                                        <div class="fw-semibold">@employee.Name</div>
                                        @* <div class="text-muted small task-description">
                                           Description
                                        </div> *@
                                      </td>
                                        <td class="align-midddle">@employee.Department</td>
                                    <td class="align-midddle">@employee.Designation</td>

                             
                                   
                                    <td class="priority-badge align-middle ">@employee.Status</td>
                                    <td class="align-middle">@employee.JoiningDate.ToString("dd MMM yyyy")</td>
                                   
                                    <td>
                                            <button class="btn btn-sm btn-edit me-2"  > <i class="fas fa-edit "></i></button>
                                            <button class="btn btn-sm btn-delete " > <i class="fas fa-trash "></i></button>

                                        
                                 </td>
                                </tr>
                                
                            }
                            
                                </tbody>
                          </table>

                        </div>

                <div class="d-flex justify-content-between align-items-center mt-4 pb-2">
                    <small class="text-muted">
                        Showing @(((currentPage - 1) * pageSize) + 1)
                        -
                        @(Math.Min(currentPage * pageSize, totalCount))
                        of @totalCount tasks
                    </small>
                    <div class="d-flex align-items-center gap-3">
                        <div class="d-flex  align-items-center gap-2 ">
                            <label class="text-muted small">Entries per page:</label>

                            <select class="form-select form-select-sm"
                                    style="width: 80px;"
                                    @bind="pageSize"
                                    @bind:after="() => { currentPage = 1; LoadEmployees(); }">
                                <option value="8">8</option>
                                <option value="12">12</option>
                                <option value="18">18</option>
                                <option value="24">24</option>
                            </select>
                        </div>
                        <nav>
                            <ul class="pagination pagination-sm mb-0 shadow-sm">

                                <!-- Previous button -->
                                <li class="page-item @(currentPage == 1 ? "disabled" : "")">
                                    <button class="page-link" @onclick="PreviousPage">Previous</button>
                                </li>


                                @foreach (var i in GetVisiblePages())
                                {
                                    <li class="page-item @(currentPage == i ? "active" : "")">
                                        <button class="page-link" @onclick="() => GoToPage(i)">@i</button>
                                    </li>
                                }


                                <!-- Next button -->
                                <li class="page-item @(currentPage == TotalPages ? "disabled" : "")">
                                    <button class="page-link" @onclick="NextPage">Next</button>
                                </li>

                            </ul>
                        </nav>
                    </div>
                </div>
                        </div>
                        </div>
                        </div>

                        </div>

@code {

    private List<EmployeeDto> pagedEmployees = new();
    private List<EmployeeDto> employee;

    private int currentPage = 1;
    private int pageSize = 8;
    private int totalCount = 0;
    private string searchTerm = "";
    private int userId;
    private string role;


    protected override async Task OnInitializedAsync()
    {
        var authState = await AuthStateProvider.GetAuthenticationStateAsync();
        var user=authState.User;
        if (user.Identity?.IsAuthenticated == true)
        {
            userId = int.Parse(user.FindFirst(ClaimTypes.NameIdentifier)?.Value ?? "0");
            role = user.FindFirst(ClaimTypes.Role)?.Value;
        }
        await LoadEmployees();
    }

    public async Task LoadEmployees()
    {
        var result = await _employeeService.GetEmployeesByUserRole(userId, role, currentPage, pageSize, searchTerm);
        pagedEmployees = result?.Items ?? new List<EmployeeDto>();
        totalCount = result?.TotalCount ?? 0;
    }

    private async Task GoToPage(int page)
    {
        currentPage = page;
        StateHasChanged();

        await LoadEmployees();
    }

    private int TotalPages => (int)Math.Ceiling((double)totalCount / pageSize);

    private async Task NextPage()
    {
        if (currentPage < TotalPages)
        {
            currentPage++;
            await LoadEmployees();
        }
    }

    private async Task PreviousPage()
    {
        if (currentPage > 1)
        {
            currentPage--;
            await LoadEmployees();
        }
    }
    List<int> GetVisiblePages()
    {
        int maxPagesToShow = 5;
        int start = Math.Max(1, currentPage - 2);
        int end = Math.Min(TotalPages, start + maxPagesToShow - 1);

        return Enumerable.Range(start, end - start + 1).ToList();
    }
    private async Task SearchTasks(ChangeEventArgs e)
    {
        searchTerm = e.Value?.ToString() ?? "";
        currentPage = 1;
        await LoadEmployees();
    }
    
}
